import { types } from '../types/types';

export const OpenModalAddMagItemsList = () => ({ type: types.magIteamListOpenModelAddImpuesto });

export const CloseModalAddMagItemsList = () => ({ type: types.magIteamListCloseModelAddImpuesto });
