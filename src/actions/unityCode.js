import { types } from '../types/types';

export const OpenModalAddUnityCode = () => ({ type: types.unityCodeOpenModelAddUnityCode });

export const CloseModalAddUnityCode = () => ({ type: types.unityCodeCloseModelAddUnityCode });
